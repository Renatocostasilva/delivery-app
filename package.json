{
  "name": "delivery-app",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.21.0",
  "scripts": {
    "dev:backend": "pnpm --filter @delivery/backend dev",
    "dev:frontend": "pnpm --filter @delivery/frontend dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  }
}